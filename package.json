{
  "name": "apollo",
  "version": "0.1.0",
  "private": true,
  "description": "Apollo — a desktop UI for agentic coding CLIs.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "pnpm run build:libs && pnpm --filter @apollo/desktop dev",
    "build": "pnpm run build:libs && pnpm --filter @apollo/desktop build",
    "build:libs": "tsc -b packages/protocol packages/core",
    "typecheck": "tsc -b --pretty",
    "typecheck:clean": "tsc -b --clean && tsc -b --pretty",
    "smoke": "pnpm run build:libs && tsx scripts/smoke.ts",
    "test": "pnpm -r --if-present test",
    "test:watch": "pnpm -r --if-present --parallel test:watch",
    "clean": "tsc -b --clean && rimraf --glob \"**/dist\" \"**/out\" \"**/.tsbuild\" \"**/*.tsbuildinfo\""
  },
  "devDependencies": {
    "@apollo/core": "workspace:*",
    "@apollo/protocol": "workspace:*",
    "@types/node": "^26.2.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.23.12",
    "typescript": "~7.0.2",
    "vitest": "^4.1.10"
  }
}
