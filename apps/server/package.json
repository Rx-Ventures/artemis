{
  "name": "@rx-artemis/server",
  "version": "0.1.0",
  "private": true,
  "description": "Headless Artemis: the HTTP server and its engine, with no window attached.",
  "type": "module",
  "main": "./dist/main.js",
  "bin": {
    "artemis-server": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@rx-artemis/core": "workspace:*",
    "@rx-artemis/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "~7.0.2",
    "vitest": "^4.1.10"
  }
}
